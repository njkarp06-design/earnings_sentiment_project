/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/earnings_sentiment_project',
  trailingSlash: true,
  images: { unoptimized: true },
};

module.exports = nextConfig;
